import { createContext } from "react";

const UserContext = createContext({

 loggedInUser :"Default User"

}); // we will give some kind information to context hold

export default UserContext ;